% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jitter_retro.R
\name{JitterFit}
\alias{JitterFit}
\title{Jitter analysis for an IMuLT fit}
\arguments{
\item{n}{Number of jittered fits.}

\item{jitter_sd}{Perturbation size. With bounds, a fraction of (upr-lwr);
without bounds, a fraction of max(|init|, min_scale).}

\item{base_seed}{Seed base; run i uses set.seed(base_seed + i) and records it.}

\item{grad_thresh}{max|grad| below which a run is flagged converged.}

\item{min_scale}{Floor on the magnitude scale when no bounds are available.}

\item{bounds}{If TRUE, look for bound fields in InitialVars and clip to them.}

\item{tol}{ΔNLL within which a run counts as having found the best mode.}

\item{rundir, append, plot}{Output controls.}

\item{...}{Forwarded to FitModel (phit, lphit, mxph, ...). Not \code{report}.}
}
\value{
(invisibly) list(summary, pars, best_nll, plot).
}
\description{
Perturbs the starting values of the active (positive-phase, non-mirrored)
parameters and re-fits \code{n} times through the full sandwich, to test whether
the model returns to the same optimum (convergence robustness / multimodality).
}
