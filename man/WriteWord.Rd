% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/WriteOutput2Word.R
\name{WriteWord}
\alias{WriteWord}
\title{Write IMuLT Output to Word Document}
\usage{
WriteWord(
  summary_dir = "Output/Summary",
  run = NULL,
  output_file = NULL,
  img_width = 6.5,
  img_height = 3.8,
  title = NULL
)
}
\arguments{
\item{summary_dir}{Path to the summary directory containing run folders.
Defaults to "Output/Summary".}

\item{run}{Character string specifying which run folder to use (e.g. "run2").
If NULL (default), uses the most recent run based on resultTable.csv timestamp.}

\item{output_file}{Name of the output Word file. Defaults to "IMuLT_Report.docx"
saved in the run directory.}

\item{img_width}{Width of images in inches (default 6.5 for portrait A4 with margins).}

\item{img_height}{Height of images in inches (default 5). Set to NULL for auto-scaling
based on the actual image aspect ratio.}

\item{title}{Optional title for the front page. If NULL, derives from the model
directory name.}
}
\value{
Invisible path to the output file.
}
\description{
Reads resultTable.csv from an IMuLT model run and assembles all plots
and tables into a formatted Word document, mirroring the HTML tab structure.
When called without arguments from the model working directory, it auto-detects
the most recent run in Output/Summary.
}
\examples{
\dontrun{
# From the model working directory - auto-detects latest run
WriteWord()

# Specify a particular run
WriteWord(run = "run2")

# Auto aspect ratio
WriteWord(img_height = NULL)

# Custom summary directory
WriteWord(summary_dir = "C:/path/to/Output/Summary", run = "run3")
}

}
